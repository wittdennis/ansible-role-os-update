---
- name: Prepare
  hosts: alpine
  gather_facts: false
  tasks:
    - name: Install python3 and sudo (missing on the base alpine image)
      ansible.builtin.raw: apk add --no-cache python3 sudo
      changed_when: false
